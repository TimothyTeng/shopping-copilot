# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtualenv
.venv/
venv/
env/

# Environment / secrets — never commit
.env
.env.*
*.key
*.pem
*secret*
*credential*

# OS cruft
.DS_Store
Thumbs.db

# Large competition data — NOT redistributed here; download separately.
# See SETUP.md. Derived from Amazon Reviews 2023 (McAuley Lab, UCSD).
techjam-conversational-search/data/catalog.jsonl
techjam-conversational-search/data/catalog.jsonl.gz
techjam-conversational-search/data/releases/

# Locally generated kit baseline results
techjam-conversational-search/results.json

# Logs / scratch
*.log

# Optional model-tier response cache (see src/backends/hyde.py)
copilot/.hyde_cache.json
copilot/.hyde_cache.tmp

# Dense retrieval build output — 77 MB, regenerated in 34 s by
# `python -m tools.build_dense`. The feature is off by default (dense_weight=0.0);
# see copilot/docs/ALGORITHMS.md 3.2 for why.
copilot/data/dense.npy
copilot/data/dense.json
