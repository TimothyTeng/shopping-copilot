"""Offline tooling: scoring harness, stress suite, demos and experiments.

Nothing here is imported by `src/` — the agent ships without it. Every module
is runnable as `python -m tools.<name>` from the `copilot/` directory.
"""
